import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#ade0e0',
      contrastText: '#323333',
    },
    text: {
      primary: '#7e69db',
    },

  },
});

export default theme;
